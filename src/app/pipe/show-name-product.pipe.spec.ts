import { ShowNameProductPipe } from './show-name-product.pipe';

describe('ShowNameProductPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowNameProductPipe();
    expect(pipe).toBeTruthy();
  });
});
