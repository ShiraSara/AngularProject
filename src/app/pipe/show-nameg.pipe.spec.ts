import { ShowNamegPipe } from './show-nameg.pipe';

describe('ShowNamegPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowNamegPipe();
    expect(pipe).toBeTruthy();
  });
});
