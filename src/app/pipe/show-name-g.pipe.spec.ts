import { ShowNameGPipe } from './show-name-g.pipe';

describe('ShowNameGPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowNameGPipe();
    expect(pipe).toBeTruthy();
  });
});
