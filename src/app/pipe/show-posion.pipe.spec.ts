import { ShowPosionPipe } from './show-posion.pipe';

describe('ShowPosionPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowPosionPipe();
    expect(pipe).toBeTruthy();
  });
});
