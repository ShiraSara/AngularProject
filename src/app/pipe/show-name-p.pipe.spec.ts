import { ShowNamePPipe } from './show-name-p.pipe';

describe('ShowNamePPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowNamePPipe();
    expect(pipe).toBeTruthy();
  });
});
