import { ShowNameCPipe } from './show-name-c.pipe';

describe('ShowNameCPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowNameCPipe();
    expect(pipe).toBeTruthy();
  });
});
