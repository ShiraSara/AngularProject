import { ShowNameSPipe } from './show-name-s.pipe';

describe('ShowNameSPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowNameSPipe();
    expect(pipe).toBeTruthy();
  });
});
